import { Role } from './Role';
import { SessionEntity } from './Session.entity';
import { User } from './User';

const entities = [User, Role];

export { User, Role, SessionEntity };
export default entities;
